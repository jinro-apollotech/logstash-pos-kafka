version: '3'
services:
  pos-kafka-logstash-matched:
    image: docker.elastic.co/logstash/logstash:8.11.0
    mem_limit: 1048m
    container_name: pos-kafka-logstash-matched
    volumes:
      - ./config:/usr/share/logstash/config
      - ./pipeline-config:/usr/share/logstash/pipeline
    ports:
      - "5047:5047"
